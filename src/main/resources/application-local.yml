server:
  port: 8081

spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clubmileage?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    mongodb:
      host: localhost
      port: 27017
      database: clubmileage
      username: root
      password: root
